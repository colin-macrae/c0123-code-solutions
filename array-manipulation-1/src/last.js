// /* exported last */
function last(array) {
  var arrayLast = 1;
  for (var i = array.length - 1; i >= array.length - 1; i--) {
    arrayLast = array[i];
  }
  return arrayLast;
}
last([1, 2, 3, 4, 5]);
