/* exported Shape */
class Shape {
  constructor(area, circumference) {
    this.area = area;
    this.circumference = circumference;
  }

  print() {
    console.log(`The area is ${this.area} and the circumference is ${this.circumference}`);
  }
}

new Shape 

// the cirle takes one parameter, square takes width, and ..
