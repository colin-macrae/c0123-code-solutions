/* exported toObject */
function toObject(keyValuePair) {
  var newObject = {};
  newObject[keyValuePair[0]] = keyValuePair[1];
  // keyValuePair[0] = ;
  // keyValuePair[1] = ;
  return newObject;
}
// console.log(toObject(['firstName', 'David']))

// create is an empty object in the function
// assign keyvaluepair at zero to the object like this: object[0] = object[1]
// assign keyvaluepair at one to the object like this:

// const array2 = {};

// array2.car = "ford";
// console.log(array2);

// newObject[keyValuePair[0]] = keyValuePair[1];
// newObject
