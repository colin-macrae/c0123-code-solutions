import App from "./App";

export default function Accordion({heading, info}) {
  return (
    <>
      <p
      className='heading one'>{heading}</p>
    </>
  )
}

// export default function Info({text}) {
//   return <p className='info two'>{info}</p>
// }
