/* exported getFirstChar */
function getFirstChar(string) {
  var firstCharIs = ' ';
  for (var i = 0; i < 1; i++) {
    firstCharIs = string[i];
  }
  return firstCharIs;
}
// console.log(getFirstChar('LearningFuze'))

// create a variable to recieve the output, make it an empty string
// loop thru the string 0 to end, at the first index
// when u find first char in string, redefine firstcharis with variable
